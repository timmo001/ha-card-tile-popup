---
name: Build

# yamllint disable-line rule:truthy
on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    uses: timmo001/workflows/.github/workflows/ha-card-build.yml@5d46351dd8a18ea5e9e0ba017da6633758bcdba3 # master
    with:
      submodules: true
