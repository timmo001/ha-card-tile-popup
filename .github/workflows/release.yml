---
name: Release

# yamllint disable-line rule:truthy
on:
  release:
    types:
      - published
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    uses: timmo001/workflows/.github/workflows/ha-card-release.yml@adea2c2b55d090414431fb13713875e588cca696 # master
    with:
      submodules: true
    permissions:
      contents: write
