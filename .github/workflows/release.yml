---
name: Release

# yamllint disable-line rule:truthy
on:
  release:
    types:
      - published
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    uses: timmo001/workflows/.github/workflows/ha-card-release.yml@2cb1c90bdea4833e142eb444efddb4aabc3cbd2f # master
    with:
      submodules: true
    permissions:
      contents: write
