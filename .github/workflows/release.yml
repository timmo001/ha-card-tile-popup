---
name: Release

# yamllint disable-line rule:truthy
on:
  release:
    types:
      - published
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    uses: timmo001/workflows/.github/workflows/ha-card-release.yml@258c64aeee76aec4a45ed2a3e54b2393460c303b # master
    with:
      submodules: true
    permissions:
      contents: write
