---
name: Release

# yamllint disable-line rule:truthy
on:
  release:
    types:
      - published
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    uses: timmo001/workflows/.github/workflows/ha-card-release.yml@29c1dbda36083c1d8d0b8ce74ede0b4acfeaf53a # master
    with:
      submodules: true
    permissions:
      contents: write
