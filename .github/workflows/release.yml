---
name: Release

# yamllint disable-line rule:truthy
on:
  release:
    types:
      - published
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    uses: timmo001/workflows/.github/workflows/ha-card-release.yml@b1733d0a2934e73b847b22acec65e75a0f113aa6 # master
    with:
      submodules: true
    permissions:
      contents: write
