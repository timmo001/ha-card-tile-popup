---
name: Release

# yamllint disable-line rule:truthy
on:
  release:
    types:
      - published
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    uses: timmo001/workflows/.github/workflows/ha-card-release.yml@747787f1187c6bb6dbbfb946e510d725660eb8bd # master
    with:
      submodules: true
    permissions:
      contents: write
