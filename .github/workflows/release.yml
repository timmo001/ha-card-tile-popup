---
name: Release

# yamllint disable-line rule:truthy
on:
  release:
    types:
      - published
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    uses: timmo001/workflows/.github/workflows/ha-card-release.yml@a579ffd827f9944e936ae691ac0b82aa5155963e # master
    with:
      submodules: true
    permissions:
      contents: write
