---
name: Release

# yamllint disable-line rule:truthy
on:
  release:
    types:
      - published
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    uses: timmo001/workflows/.github/workflows/ha-card-release.yml@ee51b59fac85756d78f89c26f2c5db58ecd5d36a # master
    with:
      submodules: true
    permissions:
      contents: write
