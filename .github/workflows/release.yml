---
name: Release

# yamllint disable-line rule:truthy
on:
  release:
    types:
      - published
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    uses: timmo001/workflows/.github/workflows/ha-card-release.yml@0a654c804d20fd738ca622e13ad1246522005b10 # master
    with:
      submodules: true
    permissions:
      contents: write
