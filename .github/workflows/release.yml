---
name: Release

# yamllint disable-line rule:truthy
on:
  release:
    types:
      - published
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    uses: timmo001/workflows/.github/workflows/ha-card-release.yml@e8abb2a494c5869c6aa88642636f735e265cdae7 # master
    with:
      submodules: true
    permissions:
      contents: write
