---
name: Release

# yamllint disable-line rule:truthy
on:
  release:
    types:
      - published
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    uses: timmo001/workflows/.github/workflows/ha-card-release.yml@4d676227c7f9080e29575e1672d90bd5a8464aca # master
    with:
      submodules: true
    permissions:
      contents: write
