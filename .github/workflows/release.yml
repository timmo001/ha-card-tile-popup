---
name: Release

# yamllint disable-line rule:truthy
on:
  release:
    types:
      - published
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    uses: timmo001/workflows/.github/workflows/ha-card-release.yml@d21c35b4f80a431a91ff02b2dd4eb5c8fd3b8cba # master
    with:
      submodules: true
    permissions:
      contents: write
