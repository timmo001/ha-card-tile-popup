---
name: Release

# yamllint disable-line rule:truthy
on:
  release:
    types:
      - published
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    uses: timmo001/workflows/.github/workflows/ha-card-release.yml@bf561d672f2ce17fa9ac1d649d356e5cce9bd40c # master
    with:
      submodules: true
    permissions:
      contents: write
