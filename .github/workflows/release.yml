---
name: Release

# yamllint disable-line rule:truthy
on:
  release:
    types:
      - published
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    uses: timmo001/workflows/.github/workflows/ha-card-release.yml@635f5727c8686b952f53d2fbe1d2e80d2480c831 # master
    with:
      submodules: true
    permissions:
      contents: write
