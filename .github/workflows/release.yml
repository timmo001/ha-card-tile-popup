---
name: Release

# yamllint disable-line rule:truthy
on:
  release:
    types:
      - published
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    uses: timmo001/workflows/.github/workflows/ha-card-release.yml@3f210701e71d6548df383f53faec30836b482fa4 # master
    with:
      submodules: true
    permissions:
      contents: write
