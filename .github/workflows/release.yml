---
name: Release

# yamllint disable-line rule:truthy
on:
  release:
    types:
      - published
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    uses: timmo001/workflows/.github/workflows/ha-card-release.yml@c932449167c52df9d447413a6daf8ab85423afa7 # master
    with:
      submodules: true
    permissions:
      contents: write
