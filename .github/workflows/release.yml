---
name: Release

# yamllint disable-line rule:truthy
on:
  release:
    types:
      - published
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    uses: timmo001/workflows/.github/workflows/ha-card-release.yml@32756bd607b44a55d23ecebcbdc06eab6c04c229 # master
    with:
      submodules: true
    permissions:
      contents: write
