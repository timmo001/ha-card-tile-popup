---
name: Release

# yamllint disable-line rule:truthy
on:
  release:
    types:
      - published
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    uses: timmo001/workflows/.github/workflows/ha-card-release.yml@d8a1fbde67c3e0e02eb592454be2e7b78ac9deb7 # master
    with:
      submodules: true
    permissions:
      contents: write
