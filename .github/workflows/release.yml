---
name: Release

# yamllint disable-line rule:truthy
on:
  release:
    types:
      - published
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    uses: timmo001/workflows/.github/workflows/ha-card-release.yml@43e7be6974bb98454ffa56fc5a4b46cc160e09bc # master
    with:
      submodules: true
    permissions:
      contents: write
