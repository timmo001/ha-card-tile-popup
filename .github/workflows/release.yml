---
name: Release

# yamllint disable-line rule:truthy
on:
  release:
    types:
      - published
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    uses: timmo001/workflows/.github/workflows/ha-card-release.yml@721f4b1e2ed4957afb95ac49968d361e18554497 # master
    with:
      submodules: true
    permissions:
      contents: write
