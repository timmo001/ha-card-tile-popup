---
name: Release

# yamllint disable-line rule:truthy
on:
  release:
    types:
      - published
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    uses: timmo001/workflows/.github/workflows/ha-card-release.yml@f879a1f9fefe5f940b9dd14cdf84e04b81c8a417 # master
    with:
      submodules: true
    permissions:
      contents: write
