---
name: Release

# yamllint disable-line rule:truthy
on:
  release:
    types:
      - published
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    uses: timmo001/workflows/.github/workflows/ha-card-release.yml@7c0c9b99547a5d90bb16f35ac848695c4b6473ab # master
    with:
      submodules: true
    permissions:
      contents: write
