---
name: Release

# yamllint disable-line rule:truthy
on:
  release:
    types:
      - published
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    uses: timmo001/workflows/.github/workflows/ha-card-release.yml@f51573e8363772976dea94bcea773012efc4a391 # master
    with:
      submodules: true
    permissions:
      contents: write
