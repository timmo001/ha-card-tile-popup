---
name: Release

# yamllint disable-line rule:truthy
on:
  release:
    types:
      - published
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    uses: timmo001/workflows/.github/workflows/ha-card-release.yml@22f512045eebd34e7f8b1cb5171a00f53c68287c # master
    with:
      submodules: true
    permissions:
      contents: write
