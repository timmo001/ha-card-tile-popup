---
name: Release

# yamllint disable-line rule:truthy
on:
  release:
    types:
      - published
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    uses: timmo001/workflows/.github/workflows/ha-card-release.yml@89463002ce025fb5be762b11083f1f26f58d76b1 # master
    with:
      submodules: true
    permissions:
      contents: write
