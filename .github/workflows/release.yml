---
name: Release

# yamllint disable-line rule:truthy
on:
  release:
    types:
      - published
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    uses: timmo001/workflows/.github/workflows/ha-card-release.yml@0ccca490086f604ff9d1c6005ee6c4338bc639b8 # master
    with:
      submodules: true
    permissions:
      contents: write
