---
name: Release

# yamllint disable-line rule:truthy
on:
  release:
    types:
      - published
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    uses: timmo001/workflows/.github/workflows/ha-card-release.yml@2482c486b3173a84a3f48934e9023e84b331e1e8 # master
    with:
      submodules: true
    permissions:
      contents: write
