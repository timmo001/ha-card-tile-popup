---
name: Release

# yamllint disable-line rule:truthy
on:
  release:
    types:
      - published
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    uses: timmo001/workflows/.github/workflows/ha-card-release.yml@7d9ccf454a6d79e49e868beba33d5b2b4a5ed850 # master
    with:
      submodules: true
    permissions:
      contents: write
