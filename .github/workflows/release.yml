---
name: Release

# yamllint disable-line rule:truthy
on:
  release:
    types:
      - published
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    uses: timmo001/workflows/.github/workflows/ha-card-release.yml@e288922717ddd852f355f8ed558370eaf074392b # master
    with:
      submodules: true
    permissions:
      contents: write
