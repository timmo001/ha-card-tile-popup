---
name: Release

# yamllint disable-line rule:truthy
on:
  release:
    types:
      - published
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    uses: timmo001/workflows/.github/workflows/ha-card-release.yml@8b435c1ef0ba93347b09ae549db256cfeaae8338 # master
    with:
      submodules: true
    permissions:
      contents: write
