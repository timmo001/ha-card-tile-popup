---
name: Release

# yamllint disable-line rule:truthy
on:
  release:
    types:
      - published
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    uses: timmo001/workflows/.github/workflows/ha-card-release.yml@524680520b97d41da35f7062e18ac6514b88fc7f # master
    with:
      submodules: true
    permissions:
      contents: write
