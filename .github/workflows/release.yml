---
name: Release

# yamllint disable-line rule:truthy
on:
  release:
    types:
      - published
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    uses: timmo001/workflows/.github/workflows/ha-card-release.yml@60872ed566f5eede3ad98cd0fb23b1d64c292497 # master
    with:
      submodules: true
    permissions:
      contents: write
