---
name: Release

# yamllint disable-line rule:truthy
on:
  release:
    types:
      - published
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    uses: timmo001/workflows/.github/workflows/ha-card-release.yml@010ccf64e49d2984bb5f0982d6ae7314114472a6 # master
    with:
      submodules: true
    permissions:
      contents: write
