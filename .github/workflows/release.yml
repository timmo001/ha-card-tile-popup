---
name: Release

# yamllint disable-line rule:truthy
on:
  release:
    types:
      - published
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    uses: timmo001/workflows/.github/workflows/ha-card-release.yml@405726c0d3241169dc241682ffa91176f9a82916 # master
    with:
      submodules: true
    permissions:
      contents: write
