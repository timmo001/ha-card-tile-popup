---
name: Release

# yamllint disable-line rule:truthy
on:
  release:
    types:
      - published
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    uses: timmo001/workflows/.github/workflows/ha-card-release.yml@aa9e6d61a254018160ae491b799b36ecdf2d114d # master
    with:
      submodules: true
    permissions:
      contents: write
