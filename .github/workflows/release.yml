---
name: Release

# yamllint disable-line rule:truthy
on:
  release:
    types:
      - published
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    uses: timmo001/workflows/.github/workflows/ha-card-release.yml@25a66b4ab492fd099bd152e0f33f7a51b4a6955f # master
    with:
      submodules: true
    permissions:
      contents: write
