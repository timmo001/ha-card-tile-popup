---
name: Release

# yamllint disable-line rule:truthy
on:
  release:
    types:
      - published
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    uses: timmo001/workflows/.github/workflows/ha-card-release.yml@3d829185994b734af661e78194e16873707456a5 # master
    with:
      submodules: true
    permissions:
      contents: write
