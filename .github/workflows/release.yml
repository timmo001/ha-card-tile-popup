---
name: Release

# yamllint disable-line rule:truthy
on:
  release:
    types:
      - published
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    uses: timmo001/workflows/.github/workflows/ha-card-release.yml@49bbe9c0795b8553b4c6cf5997406747fd42b1b7 # master
    with:
      submodules: true
    permissions:
      contents: write
