---
name: Release

# yamllint disable-line rule:truthy
on:
  release:
    types:
      - published
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    uses: timmo001/workflows/.github/workflows/ha-card-release.yml@95bc0605822adb7ca5a189e8a5c493cbb223b810 # master
    with:
      submodules: true
    permissions:
      contents: write
