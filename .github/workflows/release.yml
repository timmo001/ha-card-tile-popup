---
name: Release

# yamllint disable-line rule:truthy
on:
  release:
    types:
      - published
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    uses: timmo001/workflows/.github/workflows/ha-card-release.yml@8c9c2133c407b8f9a96839f6c2c0aa83cb90130c # master
    with:
      submodules: true
    permissions:
      contents: write
