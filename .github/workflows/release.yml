---
name: Release

# yamllint disable-line rule:truthy
on:
  release:
    types:
      - published
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    uses: timmo001/workflows/.github/workflows/ha-card-release.yml@333b75f354c288229f0e93e52114c6062a103f14 # master
    with:
      submodules: true
    permissions:
      contents: write
