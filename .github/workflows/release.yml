---
name: Release

# yamllint disable-line rule:truthy
on:
  release:
    types:
      - published
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    uses: timmo001/workflows/.github/workflows/ha-card-release.yml@6d0087dcfd2e05578eff9ca03359dbcb876d7fba # master
    with:
      submodules: true
    permissions:
      contents: write
