---
name: Release

# yamllint disable-line rule:truthy
on:
  release:
    types:
      - published
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    uses: timmo001/workflows/.github/workflows/ha-card-release.yml@663b296010c7883977baf97e6486a3417a012859 # master
    with:
      submodules: true
    permissions:
      contents: write
