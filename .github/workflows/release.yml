---
name: Release

# yamllint disable-line rule:truthy
on:
  release:
    types:
      - published
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    uses: timmo001/workflows/.github/workflows/ha-card-release.yml@5d46351dd8a18ea5e9e0ba017da6633758bcdba3 # master
    with:
      submodules: true
    permissions:
      contents: write
