---
name: Release

# yamllint disable-line rule:truthy
on:
  release:
    types:
      - published
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    uses: timmo001/workflows/.github/workflows/ha-card-release.yml@dd409a67cdb5d5b764f3fb5753df84fd4d0b2094 # master
    with:
      submodules: true
    permissions:
      contents: write
