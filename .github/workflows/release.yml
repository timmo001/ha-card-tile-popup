---
name: Release

# yamllint disable-line rule:truthy
on:
  release:
    types:
      - published
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    uses: timmo001/workflows/.github/workflows/ha-card-release.yml@dcc044376a732ab21a9718e50b0cc17aa5096e0b # master
    with:
      submodules: true
    permissions:
      contents: write
